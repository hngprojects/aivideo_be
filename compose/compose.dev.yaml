name: aivideo-dev

services:
  app:
    # env_file:
    #   - ./.env.dev
    ports:
      - 7100:7001
    volumes:
      - ../.env.dev:/app/.env

  db:
    # env_file:
    #   - ./.env.dev
    volumes:
      - ../.env.dev:/.env
