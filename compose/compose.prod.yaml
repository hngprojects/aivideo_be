name: aivideo-prod

services:
  app:
    image: ${COMPOSE_PROJECT_NAME}:latest
    env_file:
      - .env.prod
    ports:
      - 7103:7001

  db:
    volumes:
      - prod_db:/var/lib/postgresql/data
    env_file:
      - .env.prod
